class User(object):
	"""docstring for User"""
	def __init__(self, id, fullname, username, email, phonenumber, address, password):
		self.id = id
		self.fullname = fullname
		self.username = username
		self.email = email
		self.phonenumber = phonenumber
		self.address = address
		self.password = password
		